# Define the class
class Vehicle:
    # Class attributes (shared variables)
    max_speed = 200
    mileage = 100

# Accessing attributes directly using the Class Name (no object needed)
print(f"Max speed: {Vehicle.max_speed}, Mileage: {Vehicle.mileage}")